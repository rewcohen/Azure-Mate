version: '3.8'

services:
  azure-architect-mate:
    build: .
    container_name: azure-architect-mate
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production