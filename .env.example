# Azure AD App Registration Configuration
# Copy this file to .env and fill in your values
# See AZURE_AD_SETUP.md for instructions on creating an App Registration

# Required: Your Azure AD Application (Client) ID
# Get this from Azure Portal > Azure AD > App registrations > Your App > Overview
VITE_AZURE_CLIENT_ID=your-application-client-id-here

# Optional: Tenant ID (default: "organizations" for multi-tenant)
# Use "organizations" to allow any Microsoft 365 organization
# Use your specific tenant ID for single-tenant apps
VITE_AZURE_TENANT_ID=organizations

# Optional: Redirect URI (default: http://localhost:3000)
# Must match exactly what's registered in Azure Portal
VITE_AZURE_REDIRECT_URI=http://localhost:3000
